#include <stdio.h>

/**
 * main - entry point
 *
 * Description: Print lowercase alphabet backwards
 */
int main(void)
{
char x;

for (x = '0'; x <='9'; ++x)
putchar(x)
for (x = 'z'; c >= 'a'; --c)
putchar(x);

putchar('\n');

return (0);
}
